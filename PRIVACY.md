# VideoCutterStudio プライバシーポリシー
最終更新日: 2026年8月31日
バージョン: v0.1.0 対応

## 1. 基本方針
VideoCutterStudio（以下「本アプリ」）は完全オフラインで動作する動画カット・編集アプリです。開発者がユーザーの情報を外部サーバーで取得・保存・解析することは一切ありません。

## 2. アクセスする情報と利用目的
本アプリは、ユーザーが編集対象として**明示的に選択した動画ファイル**および出力先として指定したフォルダ内の情報にのみアクセスします。

具体的には以下の情報です：
- 動画ファイルのパス、ファイル名、サイズ、コーデック、解像度、長さ等のメタデータ
- カット編集のためのタイムライン情報（開始位置・終了位置）
- プレビュー表示のためのサムネイル画像生成に必要なフレームデータ

これは動画の読み込み・プレビュー・カット・書き出し機能を提供するためだけに使用し、すべての処理はご利用の端末内（ローカル）でのみ完結します。クラウドへのアップロードは行われません。
動画処理はローカルのFFmpegバイナリ呼び出しにより実行されます。

## 3. 外部送信
なし。

本アプリのソースコードには `urllib, requests, socket, http.client, smtplib` 等のネットワーク通信を行うモジュールは含まれておらず、外部へのHTTPリクエストを送信するコードは一切存在しません。
また `subprocess` 呼び出しにおいても `shell=True` は一切使用していません。
解析ツール（Google Analytics等）、広告SDK、トラッキングコードも組み込んでいません。
通知機能(plyer)およびサウンド再生はOSローカルAPIの呼び出しのみで、外部送信は行いません。

## 4. ローカルへの保存と安全管理措置
以下のデータは既定で `~/.video_cutter_studio/` 配下（今後のバージョンで設定画面から変更可能）にのみ保存され、外部に送信されることはありません。

- `settings.json`: 出力形式、解像度、エンコード設定、UI表示設定
- `recent_projects.json`: 最近開いた動画ファイル、最近の出力先フォルダ履歴
- `thumbnails_cache/`: プレビュー高速化のためのサムネイルキャッシュ
- `temp/`: 書き出し中の一時ファイル（アプリ終了時に自動削除）
- `.video_cutter_studio_location.json`: データ保存先のポインタ（ホームディレクトリ直下に固定、レジストリは不使用）

これらのファイルはOSの権限設定（0o600: 所有者のみ読み書き可相当）と、Atomic Write(fsync)により保護されます。アンインストール時にフォルダごと削除可能です。

## 5. ログについて
エラー発生時に `app_errors.log` および `ffmpeg.log` にエラー内容やエンコードログが保存されることがありますが、外部に自動送信されることは一切ありません。GitHub Issueで不具合報告する際に、ユーザーご自身の判断で一部を貼り付ける場合を除き、開発者が内容を知ることはありません。ログには動画の内容自体は含まれません。

## 6. 第三者への提供
ユーザーの動画ファイル、編集履歴、その他本アプリがアクセスする情報を第三者に提供することはありません。

## 7. お問い合わせ
本プライバシーポリシーに関するお問い合わせは、以下までお願いします。

https://github.com/Unknown777hello/Video-Cutter-Studio/issues

## 8. 変更について
本ポリシーは必要に応じて変更されることがあります。変更後のポリシーは本リポジトリ上で公開された時点から効力を生じます。
