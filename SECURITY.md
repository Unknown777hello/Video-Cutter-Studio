# Security Policy - VideoCutterStudio

## Supported Versions

セキュリティアップデートは、常に最新のリリースバージョンのみを対象とします。

| Version | Supported          |
| ------- | ------------------ |
| latest  | :white_check_mark: |
| others  | :x:                |

最新版へのアップデートを強く推奨します。

## 本アプリのセキュリティ設計

VideoCutterStudioはローカルでの動画処理に特化し、以下のセキュリティ対策を実装しています。

- **完全オフライン動作、外部送信なし**: 動画ファイルや編集情報が外部サーバーに送信されることは一切ありません。
- **shell=True 不使用**: FFmpegの呼び出しを含む全ての `subprocess` 実行で `shell=True` は使用していません。コマンドインジェクションを防止します。
- **パストラバーサル対策**: ユーザー指定のファイルパスは正規化・検証を行い、意図しない領域へのアクセスを防止します。
- **一時ファイルの安全な管理**: 書き出し時の一時ファイルは `temp/` 配下に隔離し、処理完了・アプリ終了時に確実に削除します。
- **Atomic Write採用**: 設定ファイルの保存時は一時ファイルへの書き込み + fsync + リネームによるAtomic Writeを採用し、クラッシュ時の破損を防止します。
- **依存ライブラリの最小化**: ネットワーク通信を行うライブラリ (`requests`, `urllib` 等) は含みません。

## Reporting a Vulnerability

脆弱性を発見した場合は、悪用を防ぐため公開Issueではなく、以下の方法でご報告ください。

**連絡先:** https://github.com/Unknown777hello/Video-Cutter-Studio/issues

報告時はタイトルに `[SECURITY]` を付けて作成してください。内容は自動的に制限付きで扱われます。

- 48時間以内に確認し、受領の旨を返信します
- 修正版がリリースされるまで、詳細の非公開にご協力ください
- 修正後、ご希望があれば謝辞に記載し、貢献に感謝いたします

ご協力ありがとうございます。
